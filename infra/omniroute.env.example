# OmniRoute service-only environment.
# SP Cambo provider URL/API key are NOT configured here.
# Configure SP Cambo -> OmniRoute only in Admin > Providers.
# Add only variables required by your pinned OmniRoute release.
