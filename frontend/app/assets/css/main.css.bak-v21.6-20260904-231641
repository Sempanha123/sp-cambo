@import "tailwindcss";
@import "@nuxt/ui";

/*
  SP Cambo design tokens.

  Everything visual is derived from the scales below. Pages and components must
  reuse the semantic Nuxt UI tokens (bg-default, bg-elevated, text-muted,
  border-default, text-primary, ...) instead of introducing per-page colours.

  Palette intent:
  - neutral  : deep cool navy/slate. The darkest surface is a navy, never #000.
  - primary  : restrained indigo/blue for actions and emphasis.
  - secondary: cyan/teal accent used sparingly for technical highlights.
*/
@theme static {
  --font-sans: 'Manrope Variable', 'Noto Sans Khmer Variable', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', sans-serif;
  --font-mono: 'JetBrains Mono Variable', ui-monospace, 'SFMono-Regular', Menlo, Consolas, monospace;

  /* Neutral — deep navy/slate surfaces */
  --color-slate-50: oklch(98.3% 0.004 257);
  --color-slate-100: oklch(96.5% 0.007 257);
  --color-slate-200: oklch(92.6% 0.012 257);
  --color-slate-300: oklch(86.8% 0.019 257);
  --color-slate-400: oklch(71.2% 0.029 257);
  --color-slate-500: oklch(58.2% 0.034 257);
  --color-slate-600: oklch(47.4% 0.037 257);
  --color-slate-700: oklch(39.2% 0.038 257);
  --color-slate-800: oklch(28.4% 0.035 257);
  --color-slate-900: oklch(21.8% 0.032 257);
  --color-slate-950: oklch(15.4% 0.028 257);

  /* Primary — SP Cambo indigo/blue */
  --color-indigo-50: oklch(97% 0.016 266);
  --color-indigo-100: oklch(94.2% 0.032 266);
  --color-indigo-200: oklch(89.2% 0.058 266);
  --color-indigo-300: oklch(81.6% 0.098 266);
  --color-indigo-400: oklch(71.8% 0.182 266);
  --color-indigo-500: oklch(63% 0.228 266);
  --color-indigo-600: oklch(55.6% 0.238 266);
  --color-indigo-700: oklch(48.4% 0.186 266);
  --color-indigo-800: oklch(40.8% 0.152 266);
  --color-indigo-900: oklch(34.6% 0.122 266);
  --color-indigo-950: oklch(24.2% 0.088 266);

  /* Secondary — cyan/teal technical accent */
  --color-cyan-50: oklch(98.2% 0.017 205);
  --color-cyan-100: oklch(95.4% 0.035 205);
  --color-cyan-200: oklch(91% 0.062 205);
  --color-cyan-300: oklch(85% 0.09 205);
  --color-cyan-400: oklch(76.4% 0.152 205);
  --color-cyan-500: oklch(68.4% 0.168 205);
  --color-cyan-600: oklch(59.2% 0.158 205);
  --color-cyan-700: oklch(50% 0.092 205);
  --color-cyan-800: oklch(42% 0.076 205);
  --color-cyan-900: oklch(35.6% 0.062 205);
  --color-cyan-950: oklch(25% 0.046 205);
}

@layer theme {
  :host,
  :root {
    /* Cards read as precise rather than bubbly. */
    --ui-radius: 0.5rem;
    --ui-container: 80rem;
    --ui-header-height: 4rem;

    /* Glassmorphism & Depth tokens */
  /* Glassmorphism & Depth tokens */
    --sp-blur-sm: 4px;
    --sp-blur-md: 12px;
    --sp-blur-lg: 24px;
    --sp-rim-light: color-mix(in oklab, white 12%, transparent);
    --sp-rim-gold: color-mix(in oklab, var(--sp-khmer-gold) 20%, transparent);
    --sp-shadow-ambient: 0 20px 50px -12px color-mix(in oklab, var(--ui-bg) 70%, transparent);
    --sp-shadow-focused: 0 8px 20px -6px color-mix(in oklab, var(--ui-bg) 80%, transparent);
    --sp-shadow-layered: var(--sp-shadow-ambient), var(--sp-shadow-focused);
    --sp-glow-primary: color-mix(in oklab, var(--ui-primary) 40%, transparent);
    --sp-glow-gold: color-mix(in oklab, var(--sp-khmer-gold) 40%, transparent);
    --sp-transition-premium: all 400ms cubic-bezier(0.23, 1, 0.32, 1);

    /*
      Shared code-panel semantics. Solid Nuxt UI values come first so a usable
      surface remains when a browser cannot apply the colour-mix refinements.
    */
    --sp-code-surface: var(--ui-bg-elevated);
    --sp-code-surface: color-mix(in oklab, var(--ui-bg-elevated) 96%, var(--ui-primary));
    --sp-code-header: var(--ui-bg-muted);
    --sp-code-header: color-mix(in oklab, var(--ui-bg-muted) 94%, var(--ui-primary));
    --sp-code-border: var(--ui-border-accented);
    --sp-code-border: color-mix(in oklab, var(--ui-border-accented) 84%, var(--ui-primary));
    --sp-code-foreground: var(--ui-text-highlighted);
    --sp-code-header-foreground: var(--ui-text);

    /* A quiet shell wash; localized hero glows use the stronger pair below. */
    --sp-aurora-primary-wash: transparent;
    --sp-aurora-primary-wash: color-mix(in oklab, var(--ui-primary) 8%, transparent);
    --sp-aurora-secondary-wash: transparent;
    --sp-aurora-secondary-wash: color-mix(in oklab, var(--ui-secondary) 6%, transparent);
    --sp-aurora-primary-glow: transparent;
    --sp-aurora-primary-glow: color-mix(in oklab, var(--ui-primary) 18%, transparent);
    --sp-aurora-secondary-glow: transparent;
    --sp-aurora-secondary-glow: color-mix(in oklab, var(--ui-secondary) 12%, transparent);
  }

  /*
    Dark mode is the primary experience. Nuxt UI defaults collapse muted and
    elevated onto the same value; SP Cambo separates them so panels layer
    legibly over the page background.
  */
  .dark {
    --ui-bg: var(--ui-color-neutral-950);
    --ui-bg-muted: var(--ui-color-neutral-900);
    --ui-bg-elevated: var(--ui-color-neutral-800);
    --ui-bg-accented: var(--ui-color-neutral-700);
    --ui-border: var(--ui-color-neutral-800);
    --ui-border: color-mix(in oklab, var(--ui-color-neutral-800) 78%, transparent);
    --ui-border-muted: var(--ui-color-neutral-800);
    --ui-border-muted: color-mix(in oklab, var(--ui-color-neutral-800) 55%, transparent);
    --ui-border-accented: var(--ui-color-neutral-700);

    --sp-code-surface: var(--ui-color-neutral-900);
    --sp-code-surface: color-mix(in oklab, var(--ui-color-neutral-900) 94%, var(--ui-primary));
    --sp-code-header: var(--ui-color-neutral-800);
    --sp-code-header: color-mix(in oklab, var(--ui-color-neutral-800) 96%, var(--ui-primary));
    --sp-code-border: var(--ui-color-neutral-700);
    --sp-code-border: color-mix(in oklab, var(--ui-color-neutral-700) 82%, var(--ui-primary));
    --sp-code-foreground: var(--ui-color-neutral-100);
    --sp-code-header-foreground: var(--ui-color-neutral-200);

    --sp-aurora-primary-wash: transparent;
    --sp-aurora-primary-wash: color-mix(in oklab, var(--ui-primary) 14%, transparent);
    --sp-aurora-secondary-wash: transparent;
    --sp-aurora-secondary-wash: color-mix(in oklab, var(--ui-secondary) 9%, transparent);
    --sp-aurora-primary-glow: transparent;
    --sp-aurora-primary-glow: color-mix(in oklab, var(--ui-primary) 22%, transparent);
    --sp-aurora-secondary-glow: transparent;
    --sp-aurora-secondary-glow: color-mix(in oklab, var(--ui-secondary) 14%, transparent);
  }
}

@layer base {
  html {
    scroll-behavior: smooth;
    -webkit-tap-highlight-color: transparent;
  }

  /* Keyboard focus must stay clearly visible on every interactive surface. */
  :focus-visible {
    outline: 2px solid var(--ui-primary);
    outline-offset: 2px;
    border-radius: var(--ui-radius);
  }

  ::selection {
    background-color: color-mix(in oklab, var(--ui-primary) 32%, transparent);
    color: var(--ui-text-highlighted);
  }

  /* Numeric data must stay column-aligned in tables and metric tiles. */
  table :is(td, th) {
    font-variant-numeric: tabular-nums;
  }

  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }

    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
}

@layer components {
  /* Tabular figures for balances, token counts and prices. */
  .sp-numeric {
    font-variant-numeric: tabular-nums;
    font-feature-settings: 'tnum' 1;
  }

  /* Subtle technical grid used behind hero/section headers only. */
  .sp-grid-backdrop {
    background-image:
      linear-gradient(to right, color-mix(in oklab, var(--ui-border) 60%, transparent) 1px, transparent 1px),
      linear-gradient(to bottom, color-mix(in oklab, var(--ui-border) 60%, transparent) 1px, transparent 1px);
    background-size: 56px 56px;
    mask-image: radial-gradient(ellipse 100% 70% at 50% 0%, black 20%, transparent 80%);
    opacity: 0.6;
  }

  /*
   * A static, page-level atmosphere. It deliberately lives on shells rather
   * than cards so ordinary product data stays calm and easy to scan.
   */
  .sp-shell-aurora {
    isolation: isolate;
    background-color: var(--ui-bg);
    background-image:
      radial-gradient(ellipse 78% 52% at 12% -16%, var(--sp-aurora-primary-wash), transparent 72%),
      radial-gradient(ellipse 62% 46% at 96% 8%, var(--sp-aurora-secondary-wash), transparent 74%);
    background-repeat: no-repeat;
    transition: background 1s ease-in-out;
  }

  .sp-ambient-glow {
    position: relative;
    background:
      radial-gradient(ellipse 60% 50% at 50% -10%, var(--sp-aurora-primary-glow), transparent 70%),
      radial-gradient(ellipse 40% 40% at 85% 10%, var(--sp-aurora-secondary-glow), transparent 70%);
    animation: drift-glow 20s infinite alternate ease-in-out;
  }

  @keyframes drift-glow {
    from { background-position: 0% 0%; }
    to { background-position: 2% 1%; }
  }

  @keyframes specular-shimmer {
    0% { transform: translateX(-150%) skewX(-20deg); }
    100% { transform: translateX(150%) skewX(-20deg); }
  }

  @keyframes neon-pulse {
    0%, 100% { opacity: 0.5; filter: blur(8px); }
    50% { opacity: 0.8; filter: blur(12px); }
  }

  /*
   * Code is a deliberately self-contained surface. Its foreground must not
   * inherit light-mode text utilities because the panel has its own contrast
   * contract in both colour modes.
   */
  .sp-code-block {
    overflow: hidden;
    border: 1px solid var(--sp-code-border);
    border-radius: var(--ui-radius);
    background-color: var(--sp-code-surface);
    color: var(--sp-code-foreground);
  }

  .sp-code-block__header {
    border-bottom: 1px solid var(--sp-code-border);
    background-color: var(--sp-code-header);
    color: var(--sp-code-header-foreground);
  }

  .sp-code-block__content {
    color: var(--sp-code-foreground);
  }

  /*
   * The Nuxt UI ghost button's default focus ring is translucent white, which
   * disappears on the light code header. Give this compact, code-specific
   * control a semantic focus treatment and a 44px touch target without
   * changing copy controls elsewhere in the product.
   */
  .sp-code-block__copy {
    min-width: 2.75rem;
    min-height: 2.75rem;
  }

  .sp-code-block__copy:focus-visible,
  .sp-code-block__copy[data-focus-visible] {
    /* Override Nuxt UI's translucent inverted outline inside this themed panel. */
    outline: none !important;
    box-shadow: inset 0 0 0 3px var(--ui-primary) !important;
  }

  /* Hairline divider that fades at both ends. */
  .sp-hairline {
    height: 1px;
    background: linear-gradient(to right, transparent, var(--ui-border-accented), transparent);
  }

  .sp-scroll-x {
    overflow-x: auto;
    scrollbar-width: thin;
    scrollbar-color: var(--ui-border-accented) transparent;
  }

  /*
   * Documentation prose. Tailwind Typography is not installed, so the small set of
   * element styles the docs actually need is defined here rather than repeated as
   * utility classes on every heading and paragraph.
   */
  .sp-prose {
    color: var(--ui-text-muted);
    font-size: 0.9375rem;
    line-height: 1.7;
  }

  .sp-prose > * + * {
    margin-top: 1.25rem;
  }

  .sp-prose h2 {
    margin-top: 2.5rem;
    font-size: 1.375rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--ui-text-highlighted);
    scroll-margin-top: 6rem;
  }

  .sp-prose h3 {
    margin-top: 2rem;
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--ui-text-highlighted);
    scroll-margin-top: 6rem;
  }

  .sp-prose h4 {
    margin-top: 1.5rem;
    font-weight: 600;
    color: var(--ui-text);
  }

  .sp-prose strong {
    font-weight: 600;
    color: var(--ui-text-highlighted);
  }

  .sp-prose a:not(.sp-plain) {
    color: var(--ui-primary);
    text-decoration: underline;
    text-decoration-style: dotted;
    text-underline-offset: 2px;
  }

  .sp-prose a:not(.sp-plain):hover {
    text-decoration-style: solid;
  }

  .sp-prose ul,
  .sp-prose ol {
    padding-inline-start: 1.25rem;
  }

  .sp-prose ul {
    list-style-type: disc;
  }

  .sp-prose ol {
    list-style-type: decimal;
  }

  .sp-prose li + li {
    margin-top: 0.5rem;
  }

  .sp-prose li::marker {
    color: var(--ui-text-dimmed);
  }

  .sp-prose :not(pre) > code {
    padding: 0.125rem 0.3rem;
    border-radius: calc(var(--ui-radius) * 0.75);
    background-color: var(--ui-bg-elevated);
    border: 1px solid var(--ui-border);
    font-family: var(--font-mono);
    font-size: 0.8125em;
    color: var(--ui-text-highlighted);
  }

  .sp-prose blockquote {
    padding-inline-start: 1rem;
    border-inline-start: 2px solid var(--ui-border-accented);
    color: var(--ui-text-dimmed);
  }
}

/* FIX29 — compact dashboard typography. Keep the Manrope/Noto Sans Khmer
 * pairing from the live dashboard while reducing visual density one step. */
@layer components {
  .sp-dashboard-shell {
    font-family: var(--font-sans);
    font-size: .9375rem;
  }

  .sp-dashboard-content {
    font-size: .875rem;
  }

  .sp-dashboard-content :where(.text-sm:not(.sp-numeric)) {
    font-size: .8125rem !important;
    line-height: 1.2rem !important;
  }

  .sp-dashboard-content :where(.text-base:not(.sp-numeric)) {
    font-size: .875rem !important;
    line-height: 1.3rem !important;
  }
}

/* --------------------------------------------------------------------------
 * 2026 visual refresh — Cambodian character, still product-first.
 * These motifs are CSS-only and scroll with the document. No fixed background
 * attachment is used, so the shell remains stable on mobile and browser back.
 * ------------------------------------------------------------------------ */
@layer theme {
  :root {
    --sp-khmer-gold: oklch(78% 0.14 85);
    --sp-khmer-gold-soft: color-mix(in oklab, var(--sp-khmer-gold) 14%, transparent);
    --sp-khmer-gold-line: color-mix(in oklab, var(--sp-khmer-gold) 34%, transparent);
  }

  .dark {
    --sp-khmer-gold: oklch(82% 0.13 85);
    --sp-khmer-gold-soft: color-mix(in oklab, var(--sp-khmer-gold) 12%, transparent);
    --sp-khmer-gold-line: color-mix(in oklab, var(--sp-khmer-gold) 28%, transparent);
  }
}

@layer base {
  body {
    min-height: 100svh;
    background: var(--ui-bg);
    position: relative;
  }

  body::before {
    content: '';
    position: fixed;
    inset: 0;
    opacity: 0.02;
    pointer-events: none;
    z-index: 9999;
  }
}

@layer components {
  .sp-site-header {
    border-bottom: 1px solid color-mix(in oklab, var(--ui-border) 82%, transparent);
    background: color-mix(in oklab, var(--ui-bg) 82%, transparent);
    box-shadow: 0 12px 36px color-mix(in oklab, var(--ui-bg) 78%, transparent);
    backdrop-filter: blur(18px) saturate(125%);
  }

  .sp-khmer-chip {
    display: inline-flex;
    align-items: center;
    min-height: 1.5rem;
    padding: 0.15rem 0.5rem;
    border: 1px solid var(--sp-khmer-gold-line);
    border-radius: 999px;
    background: var(--sp-khmer-gold-soft);
    color: color-mix(in oklab, var(--sp-khmer-gold) 78%, var(--ui-text-highlighted));
    font-family: 'Noto Sans Khmer Variable', 'Noto Sans Khmer', 'Khmer OS System', ui-sans-serif, system-ui, sans-serif;
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    line-height: 1;
  }

  .sp-khmer-motif {
    background-image:
      linear-gradient(135deg, transparent 38%, var(--sp-khmer-gold-line) 39% 42%, transparent 43%),
      linear-gradient(45deg, transparent 38%, color-mix(in oklab, var(--ui-primary) 22%, transparent) 39% 42%, transparent 43%);
    background-size: 28px 28px;
    background-position: 0 0, 14px 14px;
  }

  .sp-khmer-rule {
    height: 2px;
    width: 5.5rem;
    border-radius: 999px;
    background: linear-gradient(90deg, var(--ui-primary), var(--sp-khmer-gold), var(--ui-secondary));
    box-shadow: 0 0 24px color-mix(in oklab, var(--sp-khmer-gold) 26%, transparent);
  }

  .sp-gradient-text {
    color: var(--ui-text-highlighted);
    background: linear-gradient(105deg, var(--ui-primary) 0%, color-mix(in oklab, var(--sp-khmer-gold) 86%, white) 48%, var(--ui-secondary) 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .sp-premium-card {
    position: relative;
    overflow: hidden;
    border-color: color-mix(in oklab, var(--ui-border) 84%, var(--ui-primary));
    backdrop-filter: blur(var(--sp-blur-md));
    background:
      linear-gradient(110deg, transparent 40%, color-mix(in oklab, white 6%, transparent) 50%, transparent 60%),
      color-mix(in oklab, var(--ui-bg-elevated) 80%, transparent);
    background-size: 200% 100%, 100% 100%;
    box-shadow: var(--sp-shadow-layered);
    border: 1px solid var(--sp-rim-light);
    transition: var(--sp-transition-premium);
  }

  .sp-premium-card::before {
    content: '';
    position: absolute;
    inset: 0 auto auto 0;
    width: 7rem;
    height: 2px;
    background: linear-gradient(90deg, var(--ui-primary), var(--sp-khmer-gold), transparent);
    z-index: 10;
  }

  .sp-premium-card::after {
    content: '';
    position: absolute;
    inset: 0;
    opacity: 0.04;
    pointer-events: none;
    z-index: 1;
  }

  .sp-premium-card:hover {
    transform: translateY(-6px) scale(1.02);
    border-color: color-mix(in oklab, var(--ui-border-accented) 65%, var(--sp-khmer-gold));
    box-shadow: 0 32px 72px color-mix(in oklab, var(--ui-bg) 40%, transparent),
                inset 0 0 20px color-mix(in oklab, var(--ui-primary) 10%, transparent);
    background-position: 100% 0%, 0% 0%;
  }

  .sp-key-checker-card {
    background:
      radial-gradient(circle at 90% 10%, var(--sp-khmer-gold-soft), transparent 34%),
      linear-gradient(145deg, color-mix(in oklab, var(--ui-bg-elevated) 92%, var(--ui-primary)), var(--ui-bg-elevated));
  }

  .sp-auth-shell {
    min-height: 100svh;
    background-color: var(--ui-bg);
    background-image:
      radial-gradient(ellipse 70% 52% at 8% 0%, color-mix(in oklab, var(--ui-primary) 18%, transparent), transparent 70%),
      radial-gradient(ellipse 60% 42% at 100% 100%, color-mix(in oklab, var(--sp-khmer-gold) 10%, transparent), transparent 74%);
    background-repeat: no-repeat;
    background-attachment: scroll;
  }

  .sp-auth-showcase {
    position: relative;
    overflow: hidden;
    border: 1px solid color-mix(in oklab, var(--ui-border) 80%, var(--sp-khmer-gold));
    background:
      radial-gradient(circle at 20% 5%, color-mix(in oklab, var(--ui-primary) 24%, transparent), transparent 36%),
      linear-gradient(155deg, color-mix(in oklab, var(--ui-bg-elevated) 82%, var(--ui-primary)), var(--ui-bg-muted));
    box-shadow: 0 28px 80px color-mix(in oklab, var(--ui-bg) 52%, transparent);
  }

  .sp-auth-card {
    border-color: color-mix(in oklab, var(--ui-border) 78%, var(--ui-primary));
    backdrop-filter: blur(var(--sp-blur-md));
    background: color-mix(in oklab, var(--ui-bg-elevated) 85%, transparent);
    box-shadow: var(--sp-shadow-layered);
    border: 1px solid var(--sp-rim-light);
    transition: all 300ms ease-out;
  }

  .sp-dashboard-shell {
    min-height: 100svh;
    background-color: var(--ui-bg);
    background-image:
      radial-gradient(ellipse 60% 46% at 100% 0%, color-mix(in oklab, var(--ui-primary) 10%, transparent), transparent 72%),
      radial-gradient(ellipse 48% 38% at 0% 100%, color-mix(in oklab, var(--sp-khmer-gold) 6%, transparent), transparent 74%);
    background-repeat: no-repeat;
    background-attachment: scroll;
  }

  .sp-dashboard-sidebar {
    background: color-mix(in oklab, var(--ui-bg) 90%, transparent);
    backdrop-filter: blur(16px);
  }

  .sp-dashboard-page {
    position: relative;
  }

  .sp-dashboard-intro {
    position: relative;
    overflow: hidden;
    border: 1px solid color-mix(in oklab, var(--ui-border) 82%, var(--ui-primary));
    border-radius: calc(var(--ui-radius) * 1.4);
    background:
      radial-gradient(circle at 92% 0%, var(--sp-khmer-gold-soft), transparent 34%),
      linear-gradient(135deg, color-mix(in oklab, var(--ui-bg-elevated) 88%, var(--ui-primary)), color-mix(in oklab, var(--ui-bg-muted) 96%, transparent));
    padding: 1rem 1.1rem;
  }

  .sp-dashboard-intro::after {
    content: '';
    position: absolute;
    inset: 0 0 auto auto;
    width: 7rem;
    height: 3.25rem;
    opacity: 0.35;
    background-image:
      linear-gradient(135deg, transparent 38%, var(--sp-khmer-gold-line) 39% 42%, transparent 43%),
      linear-gradient(45deg, transparent 38%, color-mix(in oklab, var(--ui-primary) 20%, transparent) 39% 42%, transparent 43%);
    background-size: 22px 22px;
  }

  .sp-metric-tile {
    position: relative;
    overflow: hidden;
    border-color: color-mix(in oklab, var(--ui-border) 86%, var(--ui-primary));
    backdrop-filter: blur(var(--sp-blur-sm));
    background: color-mix(in oklab, var(--ui-bg-elevated) 80%, transparent);
    box-shadow: var(--sp-shadow-layered);
    border: 1px solid var(--sp-rim-light);
    transition: var(--sp-transition-premium);
  }

  .sp-metric-tile::after {
    content: '';
    position: absolute;
    inset: 0;
    opacity: 0.03;
    pointer-events: none;
  }

  .sp-metric-tile:hover {
    transform: translateY(-3px) scale(1.01);
    box-shadow: 0 16px 40px color-mix(in oklab, var(--ui-bg) 50%, transparent),
                inset 0 0 12px color-mix(in oklab, var(--ui-primary) 8%, transparent);
    border-color: color-mix(in oklab, var(--ui-border-accented) 65%, var(--ui-primary));
  }

  .sp-metric-tile::before {
    content: '';
    position: absolute;
    inset: 0 auto auto 0;
    width: 3.5rem;
    height: 2px;
    background: linear-gradient(90deg, var(--ui-primary), var(--sp-khmer-gold));
  }

  .sp-public-hero {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid color-mix(in oklab, var(--ui-border) 76%, transparent);
  }
  /* Collapsed dashboard rail: keep icon targets centered, readable and stable. */
  .sp-dashboard-sidebar [data-collapsed="true"] [role="listitem"],
  .sp-dashboard-sidebar[data-collapsed="true"] [role="listitem"] {
    justify-content: center;
  }

  .sp-dashboard-sidebar [data-collapsed="true"] a,
  .sp-dashboard-sidebar[data-collapsed="true"] a {
    min-width: 2.5rem;
    min-height: 2.5rem;
  }

}

/* --------------------------------------------------------------------------
 * FIX20 — Smooth premium SaaS visual system
 * Removes noisy texture/pattern backgrounds and gives every surface the same
 * restrained navy/indigo/cyan language used by the approved SP Cambo mockup.
 * ------------------------------------------------------------------------ */
@layer theme {
  :root {
    --sp-surface-0: oklch(98.8% 0.004 255);
    --sp-surface-1: oklch(97.4% 0.008 255);
    --sp-surface-2: oklch(95.8% 0.012 255);
    --sp-panel: color-mix(in oklab, white 84%, var(--ui-primary) 2%);
    --sp-panel-strong: color-mix(in oklab, white 94%, var(--ui-primary) 2%);
    --sp-panel-border: color-mix(in oklab, var(--ui-color-neutral-300) 66%, var(--ui-primary) 12%);
    --sp-soft-shadow: 0 18px 54px -34px color-mix(in oklab, var(--ui-color-neutral-950) 36%, transparent);
    --sp-card-shadow: 0 14px 36px -28px color-mix(in oklab, var(--ui-color-neutral-950) 42%, transparent);
    --sp-hover-shadow: 0 22px 48px -30px color-mix(in oklab, var(--ui-primary) 42%, transparent);
  }

  .dark {
    --sp-surface-0: #061225;
    --sp-surface-1: #08172c;
    --sp-surface-2: #0b1c34;
    --sp-panel: rgba(12, 28, 52, 0.76);
    --sp-panel-strong: rgba(13, 31, 58, 0.92);
    --sp-panel-border: rgba(135, 166, 216, 0.16);
    --sp-soft-shadow: 0 24px 64px -32px rgba(0, 0, 0, 0.72);
    --sp-card-shadow: 0 18px 48px -32px rgba(0, 0, 0, 0.72);
    --sp-hover-shadow: 0 24px 56px -34px rgba(45, 106, 255, 0.52);
  }
}

@layer base {
  body::before {
    display: none !important;
  }

  body {
    background: var(--sp-surface-0);
  }
}

@layer components {
  .sp-shell-aurora,
  .sp-dashboard-shell {
    background-color: var(--sp-surface-0) !important;
    background-image:
      radial-gradient(900px 520px at 62% -160px, color-mix(in oklab, var(--ui-primary) 15%, transparent), transparent 70%),
      radial-gradient(680px 460px at 100% 8%, color-mix(in oklab, var(--ui-secondary) 8%, transparent), transparent 72%),
      linear-gradient(180deg, var(--sp-surface-0), var(--sp-surface-1) 58%, var(--sp-surface-0)) !important;
    background-attachment: scroll !important;
  }

  .sp-site-header {
    border-bottom: 1px solid var(--sp-panel-border) !important;
    background: color-mix(in oklab, var(--sp-surface-0) 90%, transparent) !important;
    box-shadow: none !important;
    backdrop-filter: blur(20px) saturate(120%) !important;
  }

  .sp-grid-backdrop,
  .sp-khmer-motif,
  .sp-ambient-glow {
    display: none !important;
  }

  .sp-khmer-rule {
    background: linear-gradient(90deg, var(--ui-primary), var(--ui-secondary), transparent) !important;
    box-shadow: none !important;
  }

  .sp-gradient-text {
    background: linear-gradient(105deg, #55b5ff 0%, #7a8cff 44%, #9c66ff 78%, #4bd7e8 100%) !important;
    background-clip: text !important;
    -webkit-background-clip: text !important;
    -webkit-text-fill-color: transparent !important;
  }

  .sp-premium-card,
  .sp-metric-tile,
  .sp-dashboard-intro,
  .sp-smooth-panel {
    border: 1px solid var(--sp-panel-border) !important;
    background: var(--sp-panel) !important;
    box-shadow: var(--sp-card-shadow) !important;
    backdrop-filter: blur(16px) saturate(112%) !important;
  }

  .sp-premium-card::after,
  .sp-premium-card::before,
  .sp-dashboard-intro::after {
    display: none !important;
  }

  .sp-premium-card,
  .sp-metric-tile {
    transition: border-color 220ms ease, background-color 220ms ease, box-shadow 220ms ease, transform 220ms ease !important;
  }

  .sp-premium-card:hover,
  .sp-metric-tile:hover {
    transform: translateY(-2px) !important;
    border-color: color-mix(in oklab, var(--ui-primary) 42%, var(--sp-panel-border)) !important;
    box-shadow: var(--sp-hover-shadow) !important;
  }

  .sp-dashboard-sidebar {
    border-right: 1px solid var(--sp-panel-border) !important;
    background: color-mix(in oklab, var(--sp-surface-0) 93%, transparent) !important;
    backdrop-filter: blur(20px) saturate(116%);
  }

  .sp-dashboard-page {
    background: transparent !important;
  }

  .sp-dashboard-page [data-slot='header'],
  .sp-dashboard-page header {
    background: color-mix(in oklab, var(--sp-surface-0) 84%, transparent);
    backdrop-filter: blur(16px);
  }

  .sp-public-hero {
    background: transparent !important;
  }

  .sp-hero-panel {
    position: relative;
    overflow: hidden;
    border: 1px solid var(--sp-panel-border);
    border-radius: 1.4rem;
    background:
      radial-gradient(70% 120% at 82% 20%, rgba(81, 72, 255, 0.24), transparent 58%),
      radial-gradient(50% 100% at 60% 120%, rgba(21, 138, 255, 0.18), transparent 64%),
      linear-gradient(125deg, rgba(14, 46, 95, 0.95), rgba(12, 28, 61, 0.92) 48%, rgba(19, 24, 68, 0.94));
    box-shadow: var(--sp-soft-shadow);
  }

  :root:not(.dark) .sp-hero-panel {
    background:
      radial-gradient(70% 120% at 82% 20%, rgba(91, 87, 255, 0.14), transparent 58%),
      radial-gradient(50% 100% at 60% 120%, rgba(39, 151, 255, 0.12), transparent 64%),
      linear-gradient(125deg, #eef5ff, #f5f7ff 50%, #f6f4ff);
  }

  .sp-hero-orbit {
    position: relative;
    min-height: 260px;
  }

  .sp-hero-orbit::before,
  .sp-hero-orbit::after {
    content: '';
    position: absolute;
    border-radius: 999px;
    pointer-events: none;
  }

  .sp-hero-orbit::before {
    width: 220px;
    height: 220px;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    border: 1px solid rgba(112, 158, 255, 0.2);
    box-shadow: 0 0 70px rgba(80, 88, 255, 0.16) inset;
  }

  .sp-hero-orbit::after {
    width: 290px;
    height: 150px;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%) rotate(-12deg);
    border: 1px solid rgba(72, 213, 239, 0.14);
  }

  .sp-hero-core {
    position: absolute;
    left: 50%;
    top: 50%;
    z-index: 2;
    display: grid;
    place-items: center;
    width: 142px;
    height: 142px;
    transform: translate(-50%, -50%) rotate(-8deg);
    border: 1px solid rgba(135, 174, 255, 0.32);
    border-radius: 30px;
    background: linear-gradient(145deg, rgba(56, 126, 255, 0.92), rgba(66, 54, 205, 0.95));
    box-shadow: 0 30px 60px rgba(2, 8, 26, 0.4), 0 0 56px rgba(61, 130, 255, 0.26);
  }

  .sp-hero-core::before,
  .sp-hero-core::after {
    content: '';
    position: absolute;
    inset: 18px -22px -28px 22px;
    z-index: -1;
    border-radius: 26px;
    border: 1px solid rgba(114, 133, 255, 0.28);
    background: rgba(58, 67, 172, 0.46);
  }

  .sp-hero-core::after {
    inset: 34px -40px -48px 40px;
    opacity: 0.6;
  }

  .sp-floating-chip {
    position: absolute;
    z-index: 3;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 54px;
    height: 54px;
    padding: 0 12px;
    border: 1px solid rgba(139, 171, 255, 0.2);
    border-radius: 15px;
    background: rgba(14, 31, 69, 0.8);
    box-shadow: 0 16px 34px rgba(0, 0, 0, 0.24);
    color: #cfe2ff;
    backdrop-filter: blur(12px);
  }

  .sp-floating-chip--api { right: 11%; top: 18%; transform: rotate(7deg); }
  .sp-floating-chip--code { right: 5%; top: 48%; transform: rotate(8deg); }
  .sp-floating-chip--chart { left: 11%; bottom: 14%; transform: rotate(-8deg); }

  .sp-action-primary {
    box-shadow: 0 10px 28px -14px color-mix(in oklab, var(--ui-primary) 64%, transparent);
  }

  .sp-dashboard-section {
    border: 1px solid var(--sp-panel-border);
    border-radius: 1rem;
    background: color-mix(in oklab, var(--sp-panel) 76%, transparent);
    box-shadow: var(--sp-card-shadow);
    padding: 1rem;
  }

  .sp-dashboard-list {
    border: 1px solid var(--sp-panel-border) !important;
    border-radius: 1rem !important;
    background: var(--sp-panel) !important;
    box-shadow: var(--sp-card-shadow);
  }

  .sp-dashboard-list > * {
    background: transparent !important;
  }

  @media (max-width: 1023px) {
    .sp-hero-orbit { min-height: 220px; }
    .sp-hero-core { width: 118px; height: 118px; }
  }
}
@layer components {
  .sp-key-checker-card,
  .sp-buy-package-card,
  .sp-checkout-card {
    background: var(--sp-panel) !important;
  }

  /* Normalize Nuxt UI cards/panels so secondary pages inherit the same theme. */
  .sp-dashboard-shell [data-slot='root'].ring-default,
  .sp-shell-aurora [data-slot='root'].ring-default {
    border-color: var(--sp-panel-border);
  }
}

/* --------------------------------------------------------------------------
 * FIX21 — polished smooth system
 * A calmer, more realistic SaaS finish layered on top of Fix20. The goal is
 * clarity: no grain, no pattern overlays, no oversized hover movement, and a
 * consistent glass/surface hierarchy across dashboard, auth, public, tables,
 * forms, menus and dialogs.
 * ------------------------------------------------------------------------ */
@layer theme {
  :root {
    --sp-radius-card: 1rem;
    --sp-radius-hero: 1.5rem;
    --sp-line-soft: color-mix(in oklab, var(--sp-panel-border) 78%, transparent);
    --sp-line-focus: color-mix(in oklab, var(--ui-primary) 52%, var(--sp-panel-border));
    --sp-panel-hover: color-mix(in oklab, var(--sp-panel) 94%, var(--ui-primary) 6%);
    --sp-nav-hover: color-mix(in oklab, var(--ui-primary) 8%, transparent);
    --sp-nav-active: color-mix(in oklab, var(--ui-primary) 15%, transparent);
    --sp-focus-glow: 0 0 0 3px color-mix(in oklab, var(--ui-primary) 14%, transparent);
    --sp-button-glow: 0 12px 28px -15px color-mix(in oklab, var(--ui-primary) 72%, transparent);
    --sp-inset-highlight: inset 0 1px 0 color-mix(in oklab, white 7%, transparent);
  }

  .dark {
    --sp-panel-hover: rgba(17, 39, 72, 0.9);
    --sp-line-soft: rgba(132, 161, 208, 0.11);
    --sp-line-focus: rgba(91, 139, 255, 0.45);
    --sp-nav-hover: rgba(75, 117, 225, 0.08);
    --sp-nav-active: rgba(66, 111, 237, 0.16);
  }
}

@layer base {
  body {
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  button,
  a,
  input,
  textarea,
  select {
    transition-timing-function: cubic-bezier(.2,.75,.25,1);
  }
}

@layer components {
  /* Smooth global shell: intentionally static and texture-free. */
  .sp-shell-aurora,
  .sp-dashboard-shell,
  .sp-auth-shell {
    background-image:
      radial-gradient(760px 380px at 57% -120px, color-mix(in oklab, var(--ui-primary) 11%, transparent), transparent 72%),
      radial-gradient(540px 360px at 98% 14%, color-mix(in oklab, var(--ui-secondary) 6%, transparent), transparent 74%),
      linear-gradient(180deg, var(--sp-surface-0) 0%, var(--sp-surface-1) 50%, var(--sp-surface-0) 100%) !important;
  }

  /* Header / dashboard chrome. */
  .sp-site-header,
  .sp-dashboard-navbar {
    border-color: var(--sp-line-soft) !important;
    background: color-mix(in oklab, var(--sp-surface-0) 88%, transparent) !important;
    box-shadow: 0 1px 0 color-mix(in oklab, white 3%, transparent) inset !important;
    backdrop-filter: blur(18px) saturate(112%) !important;
  }

  .sp-dashboard-sidebar {
    box-shadow: 10px 0 34px -28px rgba(0, 0, 0, .8);
  }

  .sp-dashboard-sidebar nav a,
  .sp-dashboard-sidebar nav button {
    border: 1px solid transparent;
    border-radius: .75rem;
    transition: background-color 180ms ease, border-color 180ms ease, color 180ms ease, transform 180ms ease;
  }

  .sp-dashboard-sidebar nav a:hover,
  .sp-dashboard-sidebar nav button:hover {
    background: var(--sp-nav-hover);
    border-color: var(--sp-line-soft);
  }

  .sp-dashboard-sidebar nav a[aria-current='page'],
  .sp-dashboard-sidebar nav a[data-active='true'] {
    background: var(--sp-nav-active);
    border-color: color-mix(in oklab, var(--ui-primary) 28%, var(--sp-line-soft));
    box-shadow: inset 3px 0 0 var(--ui-primary);
  }

  .sp-dashboard-content {
    padding-top: .25rem;
  }

  /* Hero becomes flatter and more believable than a decorative wallpaper. */
  .sp-hero-panel {
    border-radius: var(--sp-radius-hero) !important;
    border-color: rgba(128, 158, 214, .16) !important;
    background:
      radial-gradient(520px 300px at 80% 34%, rgba(91, 82, 255, .19), transparent 68%),
      radial-gradient(420px 260px at 62% 112%, rgba(39, 160, 255, .12), transparent 72%),
      linear-gradient(125deg, rgba(14, 42, 82, .97), rgba(10, 26, 52, .96) 48%, rgba(17, 22, 58, .97)) !important;
    box-shadow: 0 22px 65px -42px rgba(0, 0, 0, .78), var(--sp-inset-highlight) !important;
  }

  :root:not(.dark) .sp-hero-panel {
    border-color: rgba(70, 107, 178, .14) !important;
    background:
      radial-gradient(520px 300px at 80% 34%, rgba(102, 95, 255, .11), transparent 68%),
      radial-gradient(420px 260px at 62% 112%, rgba(39, 160, 255, .09), transparent 72%),
      linear-gradient(125deg, #f4f8ff, #f8faff 52%, #f8f7ff) !important;
  }

  .sp-hero-core {
    box-shadow: 0 22px 54px -18px rgba(2, 8, 26, .54), 0 0 44px rgba(61, 130, 255, .18) !important;
  }

  .sp-floating-chip {
    border-color: rgba(139, 171, 255, .17) !important;
    background: rgba(12, 27, 57, .78) !important;
    box-shadow: 0 16px 36px -24px rgba(0, 0, 0, .9) !important;
  }

  /* Cards: slight depth, no shimmer, no decorative pseudo-layers. */
  .sp-premium-card,
  .sp-metric-tile,
  .sp-dashboard-section,
  .sp-dashboard-list,
  .sp-auth-card,
  .sp-auth-feature,
  .sp-smooth-panel {
    border: 1px solid var(--sp-line-soft) !important;
    background: var(--sp-panel) !important;
    box-shadow: var(--sp-card-shadow), var(--sp-inset-highlight) !important;
    backdrop-filter: blur(14px) saturate(108%) !important;
  }

  .sp-dashboard-section,
  .sp-dashboard-list,
  .sp-auth-feature {
    border-radius: var(--sp-radius-card) !important;
  }

  .sp-premium-card:hover,
  .sp-metric-tile:hover,
  .sp-command-card:hover {
    transform: translateY(-1px) !important;
    border-color: var(--sp-line-focus) !important;
    background: var(--sp-panel-hover) !important;
    box-shadow: var(--sp-hover-shadow), var(--sp-inset-highlight) !important;
  }

  .sp-command-card {
    min-height: 12.5rem;
  }

  .sp-activity-panel,
  .sp-playground-preview {
    min-width: 0;
  }

  .sp-playground-preview > div:last-child,
  .sp-playground-preview .rounded-xl {
    background: color-mix(in oklab, var(--sp-surface-0) 66%, transparent) !important;
  }

  /* Auth experience: sophisticated but calm. */
  .sp-auth-showcase {
    border: 1px solid var(--sp-line-soft) !important;
    background:
      radial-gradient(520px 340px at 16% 4%, color-mix(in oklab, var(--ui-primary) 16%, transparent), transparent 70%),
      linear-gradient(145deg, color-mix(in oklab, var(--sp-panel-strong) 94%, var(--ui-primary) 6%), var(--sp-panel)) !important;
    box-shadow: var(--sp-soft-shadow), var(--sp-inset-highlight) !important;
  }

  .sp-auth-feature {
    transition: border-color 180ms ease, background-color 180ms ease;
  }

  .sp-auth-feature:hover {
    border-color: var(--sp-line-focus) !important;
    background: var(--sp-panel-hover) !important;
  }

  .sp-public-footer {
    border-color: var(--sp-line-soft) !important;
    background: color-mix(in oklab, var(--sp-surface-0) 88%, transparent) !important;
    backdrop-filter: blur(14px);
  }

  /* Nuxt UI form fields, selects and textareas inherit one crisp surface. */
  .sp-dashboard-shell :is(input, textarea, select),
  .sp-auth-shell :is(input, textarea, select),
  .sp-shell-aurora :is(input, textarea, select) {
    border-color: var(--sp-line-soft);
  }

  .sp-dashboard-shell :is(input, textarea, select):focus,
  .sp-auth-shell :is(input, textarea, select):focus,
  .sp-shell-aurora :is(input, textarea, select):focus {
    border-color: var(--sp-line-focus);
    box-shadow: var(--sp-focus-glow);
  }

  /* Tables / data rows should scan like a real admin product. */
  .sp-dashboard-shell table tbody tr {
    transition: background-color 150ms ease;
  }

  .sp-dashboard-shell table tbody tr:hover {
    background: color-mix(in oklab, var(--ui-primary) 4%, transparent);
  }

  .sp-dashboard-list > li {
    transition: background-color 150ms ease;
  }

  .sp-dashboard-list > li:hover {
    background: color-mix(in oklab, var(--ui-primary) 4%, transparent) !important;
  }

  /* Primary actions get a subtle, realistic lift rather than neon bloom. */
  .sp-action-primary {
    box-shadow: var(--sp-button-glow) !important;
  }

  .sp-action-primary:hover {
    box-shadow: 0 14px 32px -16px color-mix(in oklab, var(--ui-primary) 82%, transparent) !important;
  }

  /* Improve scrollbars without forcing a bright track. */
  .sp-dashboard-shell,
  .sp-shell-aurora,
  .sp-auth-shell {
    scrollbar-color: color-mix(in oklab, var(--ui-border-accented) 70%, transparent) transparent;
    scrollbar-width: thin;
  }

  @media (max-width: 767px) {
    .sp-shell-aurora,
    .sp-dashboard-shell,
    .sp-auth-shell {
      background-image:
        radial-gradient(520px 280px at 70% -80px, color-mix(in oklab, var(--ui-primary) 9%, transparent), transparent 74%),
        linear-gradient(180deg, var(--sp-surface-0), var(--sp-surface-1) 55%, var(--sp-surface-0)) !important;
    }

    .sp-hero-panel {
      border-radius: 1.15rem !important;
    }

    .sp-premium-card,
    .sp-metric-tile,
    .sp-dashboard-section,
    .sp-auth-card {
      backdrop-filter: blur(10px) !important;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .sp-premium-card:hover,
    .sp-metric-tile:hover,
    .sp-command-card:hover {
      transform: none !important;
    }
  }
}

/* --------------------------------------------------------------------------
 * FIX22 — complete product-wide visual pass
 * Extends the approved smooth realistic SP Cambo direction to customer,
 * checkout, Playground, API key, order, admin, reseller and docs surfaces.
 * No texture images, moving wallpaper, grain or decorative grid backgrounds.
 * ------------------------------------------------------------------------ */
@layer theme {
  :root {
    --sp-panel-subtle: color-mix(in oklab, var(--sp-panel) 86%, transparent);
    --sp-panel-raised: color-mix(in oklab, var(--sp-panel-strong) 94%, var(--ui-primary) 2%);
    --sp-panel-selected: color-mix(in oklab, var(--sp-panel) 88%, var(--ui-primary) 12%);
    --sp-divider: color-mix(in oklab, var(--sp-panel-border) 72%, transparent);
    --sp-success-soft: color-mix(in oklab, var(--ui-success) 9%, var(--sp-panel));
    --sp-warning-soft: color-mix(in oklab, var(--ui-warning) 8%, var(--sp-panel));
    --sp-error-soft: color-mix(in oklab, var(--ui-error) 8%, var(--sp-panel));
  }

  .dark {
    --sp-panel-subtle: rgba(10, 25, 47, .68);
    --sp-panel-raised: rgba(14, 34, 63, .94);
    --sp-panel-selected: rgba(22, 49, 92, .92);
    --sp-divider: rgba(128, 158, 207, .105);
  }
}

@layer components {
  /* Every Nuxt UCard used by pages gets the same calm product surface. */
  .sp-app-card {
    border: 1px solid var(--sp-line-soft) !important;
    border-radius: var(--sp-radius-card) !important;
    background: var(--sp-panel) !important;
    box-shadow: var(--sp-card-shadow), var(--sp-inset-highlight) !important;
    backdrop-filter: blur(14px) saturate(108%) !important;
    overflow: clip;
  }

  .sp-app-card[data-variant='subtle'],
  .sp-app-card.bg-elevated\/30,
  .sp-app-card.bg-elevated\/40 {
    background: var(--sp-panel-subtle) !important;
  }

  .sp-app-card:hover {
    border-color: color-mix(in oklab, var(--ui-primary) 22%, var(--sp-line-soft)) !important;
  }

  /* Page lead is information hierarchy, not a decorative banner. */
  .sp-page-lead {
    position: relative;
    overflow: hidden;
    border-color: var(--sp-line-soft) !important;
    background: color-mix(in oklab, var(--sp-panel) 94%, var(--ui-bg) 6%) !important;
    box-shadow: inset 0 1px 0 color-mix(in oklab, white 3%, transparent) !important;
  }

  .sp-page-lead::before {
    content: '';
    position: absolute;
    inset: 0 auto 0 0;
    width: 1px;
    background: var(--ui-primary);
    opacity: .42;
  }

  .sp-page-lead-icon {
    border-color: color-mix(in oklab, var(--ui-primary) 18%, var(--sp-line-soft)) !important;
    background: color-mix(in oklab, var(--ui-primary) 6%, var(--sp-panel-raised)) !important;
    box-shadow: inset 0 1px 0 color-mix(in oklab, white 4%, transparent);
  }

  .sp-page-eyebrow {
    text-shadow: none;
  }

  /* Common legacy/raw Tailwind panels inherit the same depth without editing
     every old page one by one. */
  .sp-dashboard-content :is(
    [class~='rounded-lg'][class~='border'][class~='border-default'],
    [class~='rounded-xl'][class~='border'][class~='border-default'],
    [class~='rounded-2xl'][class~='border'][class~='border-default']
  ) {
    border-color: var(--sp-line-soft) !important;
  }

  .sp-dashboard-content :is(
    [class~='bg-elevated/20'],
    [class~='bg-elevated/30'],
    [class~='bg-elevated/35'],
    [class~='bg-elevated/40']
  ) {
    background-color: var(--sp-panel-subtle) !important;
  }

  /* Product / buy cards: polished selectable tiles. */
  .sp-dashboard-content button[class~='border'][class~='p-4'][class~='text-left'] {
    border-radius: .95rem !important;
    border-color: var(--sp-line-soft) !important;
    background: var(--sp-panel-subtle);
    box-shadow: inset 0 1px 0 color-mix(in oklab, white 4%, transparent);
    transition: border-color 170ms ease, background-color 170ms ease, box-shadow 170ms ease, transform 170ms ease !important;
  }

  .sp-dashboard-content button[class~='border'][class~='p-4'][class~='text-left']:hover {
    transform: translateY(-1px);
    border-color: var(--sp-line-focus) !important;
    background: var(--sp-panel-hover);
  }

  .sp-dashboard-content button[class~='border-primary'] {
    background: var(--sp-panel-selected) !important;
    box-shadow: inset 0 0 0 1px color-mix(in oklab, var(--ui-primary) 18%, transparent), var(--sp-card-shadow) !important;
  }

  /* Checkout / payment area: place payment information on a stronger surface. */
  .sp-checkout-card,
  .sp-buy-package-card,
  .sp-key-checker-card {
    border: 1px solid var(--sp-line-soft) !important;
    border-radius: 1rem !important;
    background: var(--sp-panel-raised) !important;
    box-shadow: var(--sp-card-shadow), var(--sp-inset-highlight) !important;
  }

  .sp-dashboard-content :is(.bg-success\/5, .bg-success\/10) {
    background: var(--sp-success-soft) !important;
  }

  .sp-dashboard-content :is(.bg-warning\/5, .bg-warning\/10) {
    background: var(--sp-warning-soft) !important;
  }

  .sp-dashboard-content :is(.bg-error\/5, .bg-error\/10) {
    background: var(--sp-error-soft) !important;
  }

  /* API keys and order rows read like production data, not stacked boxes. */
  .sp-dashboard-content .divide-y.divide-default,
  .sp-dashboard-content dl.divide-y.divide-default {
    border-color: var(--sp-line-soft) !important;
    border-radius: 1rem;
    background: var(--sp-panel-subtle);
    box-shadow: inset 0 1px 0 color-mix(in oklab, white 3%, transparent);
  }

  .sp-dashboard-content .divide-y.divide-default > * {
    border-color: var(--sp-divider) !important;
  }

  .sp-dashboard-content .divide-y.divide-default > :is(a, button, div) {
    transition: background-color 150ms ease, border-color 150ms ease;
  }

  .sp-dashboard-content .divide-y.divide-default > :is(a, button, div):hover {
    background: color-mix(in oklab, var(--ui-primary) 4%, transparent);
  }

  /* Forms: consistent height, quieter borders and readable focus. */
  .sp-dashboard-shell :is(input, textarea, select),
  .sp-auth-shell :is(input, textarea, select),
  .sp-shell-aurora :is(input, textarea, select) {
    background-color: color-mix(in oklab, var(--sp-surface-0) 56%, var(--sp-panel)) !important;
    box-shadow: inset 0 1px 0 color-mix(in oklab, white 3%, transparent);
  }

  .sp-dashboard-shell textarea,
  .sp-auth-shell textarea,
  .sp-shell-aurora textarea {
    line-height: 1.55;
  }

  /* Menus/popovers/dialogs should not switch to a harsher visual language. */
  [role='menu'],
  [role='listbox'],
  [data-reka-popper-content-wrapper] > div {
    border-color: var(--sp-line-soft) !important;
    background-color: var(--sp-panel-strong) !important;
    box-shadow: 0 24px 64px -30px rgba(0, 0, 0, .72), var(--sp-inset-highlight) !important;
    backdrop-filter: blur(18px) saturate(110%);
  }

  /* Tables: one calm sheet, subtle row separation, sticky headers remain legible. */
  .sp-dashboard-content table {
    border-collapse: separate;
    border-spacing: 0;
  }

  .sp-dashboard-content table thead th {
    border-bottom-color: var(--sp-divider) !important;
    background: color-mix(in oklab, var(--sp-panel-raised) 90%, transparent);
    color: var(--ui-text-muted);
    font-size: .72rem;
    font-weight: 650;
    letter-spacing: .055em;
    text-transform: uppercase;
  }

  .sp-dashboard-content table tbody td {
    border-bottom-color: var(--sp-divider) !important;
  }

  .sp-dashboard-content table tbody tr:last-child td {
    border-bottom-color: transparent !important;
  }

  /* Code, key and raw-response panels use a refined terminal surface. */
  .sp-code-block,
  .sp-dashboard-content pre,
  .sp-dashboard-content code[class*='block'] {
    border-color: color-mix(in oklab, var(--sp-line-soft) 86%, var(--ui-primary) 8%) !important;
  }

  .sp-dashboard-content pre {
    border-radius: .9rem;
    background: var(--sp-code-surface) !important;
    color: var(--sp-code-foreground) !important;
    box-shadow: inset 0 1px 0 color-mix(in oklab, white 3%, transparent);
  }

  /*
   * Playground code must remain readable in both color schemes. Earlier builds
   * mixed a light-mode pre background with a hard-coded light text utility,
   * which produced the washed-out block seen in the customer screenshot.
   */
  .sp-playground-code {
    background: var(--sp-code-surface) !important;
    color: var(--sp-code-foreground) !important;
  }

  .sp-playground-code .sp-code-block__header {
    background: var(--sp-code-header) !important;
    color: var(--sp-code-header-foreground) !important;
  }

  .sp-playground-code pre,
  .sp-playground-code code {
    color: inherit !important;
  }

  /* Playground: chat canvas, run setup and telemetry read as one workspace. */
  .sp-dashboard-content .sp-premium-card.overflow-hidden {
    background: color-mix(in oklab, var(--sp-panel) 95%, var(--sp-surface-0) 5%) !important;
  }

  .sp-dashboard-content [data-playground-composer] {
    min-height: 5.5rem;
    resize: vertical;
  }

  /* Nuxt UI buttons: make primary actions crisp and secondary actions flatter. */
  .sp-dashboard-shell button,
  .sp-shell-aurora button,
  .sp-auth-shell button {
    border-radius: .72rem;
  }

  .sp-dashboard-shell button:not(:disabled):active,
  .sp-shell-aurora button:not(:disabled):active,
  .sp-auth-shell button:not(:disabled):active {
    transform: translateY(1px);
  }

  /* Admin metric grids and operation cards get stronger scan hierarchy. */
  .sp-dashboard-content .grid > .sp-app-card,
  .sp-dashboard-content .grid > .sp-premium-card {
    min-width: 0;
  }

  .sp-dashboard-content :is(h2, h3).font-semibold,
  .sp-dashboard-content :is(h2, h3).font-medium {
    letter-spacing: -.012em;
  }

  /* Scrollbar inside dense lists/tables. */
  .sp-dashboard-content * {
    scrollbar-color: color-mix(in oklab, var(--ui-border-accented) 56%, transparent) transparent;
    scrollbar-width: thin;
  }

  @media (hover: hover) and (pointer: fine) {
    .sp-app-card {
      transition: border-color 180ms ease, background-color 180ms ease, box-shadow 180ms ease;
    }

    .sp-app-card:hover {
      background: var(--sp-panel-hover) !important;
      box-shadow: var(--sp-hover-shadow), var(--sp-inset-highlight) !important;
    }
  }

  @media (max-width: 639px) {
    .sp-dashboard-content {
      padding-inline: .125rem;
    }

    .sp-page-lead {
      border-radius: .9rem !important;
    }

    .sp-app-card,
    .sp-dashboard-section,
    .sp-dashboard-list {
      border-radius: .9rem !important;
    }

    .sp-dashboard-content table thead th {
      font-size: .68rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .sp-dashboard-content button[class~='border'][class~='p-4'][class~='text-left']:hover,
    .sp-dashboard-shell button:not(:disabled):active,
    .sp-shell-aurora button:not(:disabled):active,
    .sp-auth-shell button:not(:disabled):active {
      transform: none !important;
    }
  }
}

@layer components {
  .sp-catalog-option,
  .sp-api-key-row,
  .sp-admin-record,
  .sp-admin-mini-card,
  .sp-admin-callout {
    border-color: var(--sp-line-soft) !important;
    background: var(--sp-panel-subtle) !important;
    box-shadow: inset 0 1px 0 color-mix(in oklab, white 3%, transparent);
  }

  .sp-catalog-option {
    min-height: 10.75rem;
  }

  .sp-catalog-option[aria-checked='true'] {
    border-color: var(--sp-line-focus) !important;
    background: var(--sp-panel-selected) !important;
    box-shadow: inset 0 0 0 1px color-mix(in oklab, var(--ui-primary) 18%, transparent), var(--sp-card-shadow) !important;
  }

  .sp-api-key-row,
  .sp-admin-record,
  .sp-admin-mini-card {
    transition: border-color 170ms ease, background-color 170ms ease, box-shadow 170ms ease;
  }

  .sp-api-key-row:hover,
  .sp-admin-record:hover,
  .sp-admin-mini-card:hover {
    border-color: color-mix(in oklab, var(--ui-primary) 26%, var(--sp-line-soft)) !important;
    background: var(--sp-panel-hover) !important;
  }

  .sp-admin-callout {
    background: color-mix(in oklab, var(--sp-panel-subtle) 92%, var(--ui-primary) 8%) !important;
  }
}

/* FIX28 — viewport Playground + responsive composer/history refinements. */
@layer components {
  .sp-dashboard-content:has(.sp-playground-shell) {
    width: 100%;
    max-width: 1600px !important;
    padding-bottom: .25rem !important;
  }

  .sp-playground-shell {
    overscroll-behavior: contain;
    contain: layout paint;
  }

  .sp-playground-shell [data-playground-composer] {
    min-height: 2.75rem !important;
    max-height: 10rem !important;
    resize: none !important;
    overflow-y: auto !important;
  }

  @media (max-width: 639px) {
    .sp-dashboard-content:has(.sp-playground-shell) {
      padding-inline: 0 !important;
    }

    .sp-playground-shell [data-playground-composer] {
      min-height: 2.7rem !important;
      max-height: 7.5rem !important;
      font-size: 16px !important;
      line-height: 1.45 !important;
    }
  }
}

/* FIX28 — compact premium navigation + production Playground composer polish. */
@layer components {
  .sp-dashboard-sidebar {
    background: color-mix(in oklab, var(--sp-surface-0) 96%, transparent) !important;
    box-shadow: 8px 0 30px -28px rgba(0, 0, 0, .76) !important;
  }

  .sp-dashboard-nav-groups {
    display: flex;
    flex-direction: column;
    gap: .8rem;
    padding: .15rem .25rem .75rem;
  }

  .sp-dashboard-nav-group {
    min-width: 0;
  }

  .sp-dashboard-nav-label {
    margin: 0 .55rem .3rem;
    color: color-mix(in oklab, var(--ui-text-muted) 88%, transparent);
    font-size: .625rem;
    font-weight: 650;
    letter-spacing: .085em;
    line-height: 1rem;
    text-transform: uppercase;
  }

  .sp-dashboard-nav-menu {
    margin-inline: -.15rem;
  }

  .sp-dashboard-sidebar nav a,
  .sp-dashboard-sidebar nav button {
    min-height: 2.25rem;
    border-color: transparent !important;
    border-radius: .62rem !important;
    background: transparent !important;
    box-shadow: none !important;
    transition: background-color 140ms ease, color 140ms ease, border-color 140ms ease !important;
  }

  .sp-dashboard-sidebar nav a:hover,
  .sp-dashboard-sidebar nav button:hover {
    border-color: transparent !important;
    background: color-mix(in oklab, var(--ui-primary) 5%, transparent) !important;
    box-shadow: none !important;
    transform: none !important;
  }

  .sp-dashboard-sidebar nav a[aria-current='page'],
  .sp-dashboard-sidebar nav a[data-active='true'] {
    border-color: color-mix(in oklab, var(--ui-primary) 22%, transparent) !important;
    background: color-mix(in oklab, var(--ui-primary) 10%, transparent) !important;
    box-shadow: inset 0 0 0 1px color-mix(in oklab, var(--ui-primary) 8%, transparent) !important;
  }

  .sp-dashboard-sidebar nav a[aria-current='page']:hover,
  .sp-dashboard-sidebar nav a[data-active='true']:hover {
    background: color-mix(in oklab, var(--ui-primary) 12%, transparent) !important;
  }

  .sp-playground-shell [data-playground-composer] textarea,
  .sp-playground-shell textarea[data-playground-composer] {
    border: 0 !important;
    background: transparent !important;
    box-shadow: none !important;
  }

  @media (max-width: 639px) {
    .sp-dashboard-nav-groups {
      gap: .65rem;
    }
  }
}
